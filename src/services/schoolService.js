export const schoolService = {
    getAll: async () => {
        // retorna await api.get('/schools')
    },
    create: async (data) => {
        // return await api.post('/schools', data)
    },
    update: async (id, data) => {
        // return await api.put(`/schools/${id}`, data)
    },
    delete: async (id) => {
        // return await api.delete(`/schools/${id}`)
    }
}